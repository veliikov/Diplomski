{
  "name": "finalproject",
  "private": true,
  "description": "NFT marketplace diploma project",
  "scripts": {
    "install:all": "npm install --prefix wallet-toolkit && npm install --prefix nikola",
    "start": "npm run start --prefix nikola",
    "build:toolkit": "npm run build --prefix wallet-toolkit"
  }
}
